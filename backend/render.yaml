serviceType: web
env: node
buildCommand: npm install
startCommand: npm start
plan: starter
branch: main
envVars:
  - key: PORT
    value: "5000"
  - key: MONGODB_URI
    sync: false
  - key: JWT_SECRET
    sync: false
  - key: NODE_ENV
    value: production
healthCheckPath: /health
autoDeploy: true